package com.example.rawmac.Model

class Model (
    var email: String?, // getter and setter methods
    // for password variable
    var password: String?
    ) {

    // constructor to initialize
    // the variables
    init {
        email = email
        password = password
    }
}